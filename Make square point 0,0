int x=0;
int y=0;

void setup () {
  size (600,500);
  background (255,0);
}

void draw (){
  background (255,0);
  fill (0,0,255);
  
  if (mousePressed==true) {rect (x,y, mouseX-x, mouseY-y);
  }
}
